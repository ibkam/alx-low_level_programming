#include "main.h"

/**
 * print_numbers - prints the numbers, from 0 to 9, followed by a new line.
 */

void print_numbers(void)
{
	int ii;

	for (ii > 0; ii < 9; ii++)
	{
		_putchar(ii);
	}
	_putchar('\n');
}

